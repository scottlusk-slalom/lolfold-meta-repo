# Project Repositories Configuration
# Validate with: ./scripts/validate-repos-yaml.sh

repositories:
  # example-api:
  #   purpose: Backend API service
  #   description: >
  #     Primary REST API serving the frontend application.
  #   local_path: repos/example-api
  #   default_gate_level: minimal    # minimal | standard | full
  #   status: proposed               # proposed | planned | active | legacy | archived
  #   git:
  #     organization: <YOUR_ORG>
  #     repository: example-api
  #     clone_url: https://github.com/<YOUR_ORG>/example-api.git
  #     default_branch: main
  #   when_to_use:
  #     - Working on backend API features
  #     - Modifying REST endpoints

selection_guidelines:
  # Add guidelines for when to select each repository for a spec
  # example-api: Use for API feature work, bug fixes, and endpoint changes
